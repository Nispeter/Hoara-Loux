using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy_02 : EnemyController
{
    public int score;

    void Start()
    {
        setScoreAdd(score);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
